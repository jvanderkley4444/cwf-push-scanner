{
  "//": "Field-override indexes the scanner needs. feed.timestamp and posts.ts get a COLLECTION_GROUP index (for the scanner's cross-parent collectionGroup queries) AND keep their COLLECTION-scope asc/desc indexes (which the in-app per-parent feed/club listeners orderBy). Creating an override turns off automatic indexing for that field, so the COLLECTION entries below MUST stay or the app's feed/club queries break. Deploy:  firebase deploy --only firestore:indexes --project cookwithfriends-3b40e",
  "indexes": [],
  "fieldOverrides": [
    {
      "collectionGroup": "feed",
      "fieldPath": "timestamp",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "DESCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" },
        { "order": "DESCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    },
    {
      "collectionGroup": "posts",
      "fieldPath": "ts",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "DESCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" },
        { "order": "DESCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    }
  ]
}
